function NumberHolder(n) {
    return {
        value: n,
        marked: false
    };
}

filterNumbersByMultiplesOfP (numbers, p) {
    return numbers.filter (function (number) {
        return number 
    });
}

function find_last_prime_within_n(n) {
    if (n < 2) {
        throw Error ("n cannot be less than 2");
    }
    let numbers = [];
    for (let i = 1; i < n; i++) {
        numbers = new NumberHolder(n);
    }
    let p = 2;
    
    for (;;) {
        
    }
}

find_last_prime_within_n (input.getValueOfN());
